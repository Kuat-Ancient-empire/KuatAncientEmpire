success_text = {
	check_variable = {
		which = $TYPE$_class_queue
		value > 0
	}
	check_variable = {
		which = $TYPE$_class_queue
		value <= event_target:global_event_country.kuat_shipyard_buillding_queue_selection
	}
	text = kuat_shipyard.$ID$.b.desc
}

success_text = {
	check_variable = {
		which = $TYPE$_class_queue
		value > event_target:global_event_country.kuat_shipyard_buillding_queue_selection
	}
	text = kuat_shipyard.$ID$.a5.desc
}

success_text = {
	OR = {
		NOT = {
			is_variable_set = $TYPE$_class_queue
		}
		check_variable = {
			which = $TYPE$_class_queue
			value = 0
		}
	}
	text = kuat_shipyard.$ID$.a.desc
}
