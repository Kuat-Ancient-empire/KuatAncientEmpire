potential = {
	from = {
		is_scope_type = country
		has_country_flag = kuat_build_fifth_ships
		has_country_flag = building_the_$TYPE$
		has_country_flag = build_all_sell_ship_for_design_fleet
	}
}

allow = {
	from = {
		hidden_trigger = {
			is_scope_type = country
		}
		custom_tooltip = {
			fail_text = kuat_shipyard.check
			get_ship_alloys_allow = {
				$TYPE$ = yes
			}
		}
		custom_tooltip = {
			fail_text = kuat_shipyard.uppon_fleet
			check_variable_arithmetic = {
				which = value:kuat_shipyard_exist_commandline
				value < value:kuat_shipyard_command_line
			}
		}
	}
}

effect = {
	custom_tooltip = "exe_shipyard.a"
	hidden_effect = {
		from = {
			set_country_flag = fired_$TYPE$
			change_variable = {
				which = $TYPE$_queue
				value = event_target:global_event_country.kuat_shipyard_buillding_queue_selection
			}
			set_variable = {
				which = mulf_$TYPE$_queue
				value = $TYPE$_queue
			}
			multiply_variable = {
				which = mulf_$TYPE$_queue
				value = $SIZE$
			}
			get_alloys_variable = {
				BUY = yes
				$TYPE$ = yes
			}
		}
	}
}
