potential = {
	from = {
		is_scope_type = country
		has_country_flag = kuat_build_fifth_ships
		has_country_flag = building_the_$TYPE$
		has_country_flag = build_all_sell_ship_for_design_fleet
	}
}

allow = {
	from = {
		hidden_trigger = {
			is_scope_type = country
		}
		custom_tooltip = {
			fail_text = kuat_shipyard_queue
			check_variable = {
				which = $TYPE$_queue
				value > 0
			}
		}
	}
}

effect = {
	custom_tooltip = kuat_shipyard.confirm.a.tooltip
	hidden_effect = {
		FROM = {
			set_variable = {
				which = $TYPE$_class_queue
				value = 0
			}
			initial_shipyard_output = {
				TYPE = $TYPE$_true_numbers
				SLOT = $TYPE$_queue
				SCREEN = $TYPE$_class_queue
				DAYS = @kuat_build_time
				DAYS_1 = @kuat_build_time_1
				OUTPUT_ID = $ORDER$1
				CHECK_ID = $ORDER$2
				TEMPLE_FLAG = fired_$TYPE$
			}
		}
	}
}
