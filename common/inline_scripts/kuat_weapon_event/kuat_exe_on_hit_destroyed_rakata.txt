on_hit = {
	custom_tooltip_with_params = {
		description = "kuat_weapon_erase_shield_reduce_on_hit_effect"
		description_parameters = {
			"var_1" = $kuat_weapon_stollen_value$
			"var_2" = $kuat_weapon_stollen_days$
		}
	}
	hidden_effect = {
		if = {
			limit = { check_modifier_value = { modifier = ship_shield_hardening_add value > 0 } }
			kuat_battle_steal_modifier_on_hit = {
				VARIABLE = infinite_ship_damage_reduce_reduce_temp
				KUAT_MODIFIER = kuat_weapon_steal_ship_shield
				MODIFIER = kuat_shield_steal_weapon_hit_modifier
				AMOUNT = $kuat_weapon_stollen_value$
				DAYS = $kuat_weapon_stollen_days$
			}
		}
	}
}
