id = $ID$
hide_window = yes
is_triggered_only = yes
trigger = { has_fleet_flag = is_modifiering_fleet } #
immediate = {  
	if = {
		limit = { is_in_combat = yes }
		every_owned_ship = { 
			$KEY$ = { frendly = yes }
			set_ship_flag = kuat_is_modifiering 
			set_confused = no remove_terrified = yes
		}
		fleet_event = { id = $ID$ days = 2 }
	}
	else = { kuat_battle_clear_battleship_modifier_fixed_ship = yes }
}