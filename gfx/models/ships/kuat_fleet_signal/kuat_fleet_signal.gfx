objectTypes = {
	pdxmesh = {
		name = "kuat_fleet_signal_mesh"
		file = "gfx/models/ships/kuat_fleet_signal/kuat_fleet_signal.mesh"
		#jump first
		animation = { id = idle type = kuat_fleet_signal_idle_animation }
		#static
		animation = { id = idle1 type = kuat_fleet_signal_idle1_animation }
		#jump mid
		animation = { id = idle2 type = kuat_fleet_signal_idle2_animation }
		animation = { id = idle3 type = kuat_fleet_signal_idle3_animation }
		#death
		animation = { id = idle4 type = kuat_fleet_signal_idle4_animation }
		#jump slow
		animation = { id = idle5 type = kuat_fleet_signal_idle5_animation }
		animation = { id = idle6 type = kuat_fleet_signal_idle6_animation }
		animation = { id = idle7 type = kuat_fleet_signal_idle7_animation }
		
		meshsettings = {
			name = "pCubeShape1"
			index = 0
			texture_diffuse = "nodiffuse.dds"
			texture_normal = "nonormal.dds"
			texture_specular = "nospec.dds"
			shader = "Collision"
		}
	}
}
